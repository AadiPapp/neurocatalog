# NeuroCatalog – Instant Neuroscience Data Portal (built on Neuralink datarepo)

Run:
```bash
pip install -r requirements.txt
python build.py   # → creates ./public with full static site + dummy data demo
